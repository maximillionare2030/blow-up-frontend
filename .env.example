API_BASE_URL=http://localhost:8000
# Server-only values. Never use NEXT_PUBLIC_ for these.
# Contact inquiries are stored by the backend; these only power the
# best-effort notification email and may be left empty.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
